# Outlook Worker

Planned responsibilities:

- draft email
- send email after approval
- watch mailbox for replies
- normalize incoming email into case events

Preferred runtime:

- Classic Outlook with COM/OOM

Fallback:

- Graph if policy allows
- UI automation only if required
