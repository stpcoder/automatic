# Workflow Registry Package

This package should load and validate:

- workflow yaml
- system registry yaml
- templates
- checklists
- approval policies

It should provide versioned workflow access to the orchestrator.
